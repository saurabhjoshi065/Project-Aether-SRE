route:
  group_by: ['alertname']
  group_wait: 5s
  group_interval: 5s
  repeat_interval: 1h
  receiver: 'agent-service'

receivers:
  - name: 'agent-service'
    webhook_configs:
      - url: 'http://agent-service:8000/v1/alerts'
        send_resolved: true